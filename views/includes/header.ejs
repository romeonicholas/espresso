<header class="header">
    <h1><a href="/">Your Best Shots</a></h1>
    <span class="header-links">
        <% if (typeof username !== 'undefined') { %>
            <span><strong class="hidden-mobile">[<%= username %>]</strong> <a href="/users/logout">Log Out</a></span>&#32;&#32;
        <% } else { %>
            <a href="/users/new" class="hidden-mobile">Sign Up</a>&nbsp;&nbsp;
            <a href="/users/login">Log In</a>&nbsp;&nbsp;
        <% } %>

        <% if (typeof isAdmin !== 'undefined' && isAdmin === true) { %>
             <span class="hidden-mobile"><a href="/admin/unpublished">admin</a></span>&#32;&#32;
        <% } %>
            <label class="switch">
                <input type="checkbox" class="check">
                <span class="slider round"></span>
            </label>
    </span>
</header>